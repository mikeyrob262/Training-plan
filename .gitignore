.persona-build/
node_modules/
.syntax-check/
.sc/

live_now.html

# Layout-verification screenshots. Regenerated by scripts/legacy-run-layout-measure.mjs;
# they are evidence for a session, not source, and they are ~1MB a run.
.layout-shots/

# wrangler build/dev temp output - never source
.wrangler/tmp/

# wrangler local dev state - a 4MB sqlite WAL that changes on every dev run
.wrangler/state/
